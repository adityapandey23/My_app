package com.example.my_app.kotlinexs

// POJO -> plain old java object / Vo value object/ model class -- box with partitions
class Student(// For getting and setting the value of class variables
    var name: String, var age: Int, var postalAddress: String
)
